# Base URL of the CloakPipe server-mode instance the admin UI manages.
# The admin API is only available when CloakPipe runs as: cloakpipe start server
# Leave empty to use same-origin (recommended when served by `npx cloakpipe serve`).
VITE_CLOAKPIPE_BASE_URL=http://127.0.0.1:8400
